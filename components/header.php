<header class="header">

   <section class="flex">
      <a href="#" class="logo">Logo</a>

      <nav class="navbar">
         <a href="supply_company.php">Firma Tanımla</a>
         <a href="supply_company_check.php">Firmalar</a>
         <a href="orders.php">my orders</a>
         <?php
         /*                         $count_cart_items = $conn->prepare("SELECT * FROM `cart` WHERE username = ?");
                        $count_cart_items->execute([$user_name]);
                        $total_cart_items = $count_cart_items->rowCount(); */
         ?>
         <!-- <a href="shopping_cart.php" class="cart-btn">cart<span></span></a> --> <!-- $total_cart_items; -->
      </nav>

      <div id="menu-btn" class="fas fa-bars"></div>
   </section>

</header>